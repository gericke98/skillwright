{
  "name": "@skillwright/evals",
  "version": "0.0.0",
  "description": "skillwright distiller eval suite — golden recordings scored against the M2 rubric",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  },
  "dependencies": {
    "skillwright": "workspace:*",
    "@skillwright/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
