{
  "name": "@skillwright/extension",
  "version": "0.0.0",
  "description": "skillwright Chrome extension — capture (content scripts) + CDP relay client",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "build:crx": "vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "@skillwright/shared": "workspace:*"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.1",
    "@types/chrome": "^0.2.2",
    "vite": "^8.1.3"
  }
}
