{
  "name": "@bskill/extension",
  "version": "0.0.0",
  "description": "bskill Chrome extension — capture (content scripts) + CDP relay client",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  },
  "dependencies": {
    "@bskill/shared": "workspace:*"
  }
}
